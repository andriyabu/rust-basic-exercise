fn main() {
    // variable with predefined value
    let variable_name = "predefined value";

    // {} called string literal
    // printl!() is not a function but macro identified by ! signed
    println!("{}", variable_name);
}
